apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: saleor-db
  name: saleor-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
